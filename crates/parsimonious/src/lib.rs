//! Core Rust crate for parsimonious-rs.
//!
//! This crate is intentionally kept independent from the Python bindings.

#![forbid(unsafe_code)]

/// Placeholder function while the crate is being implemented.
pub fn placeholder() -> &'static str {
    "parsimonious core crate (WIP)"
}
